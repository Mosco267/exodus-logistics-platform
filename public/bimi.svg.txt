import createMiddleware from "next-intl/middleware";

export default createMiddleware({
  locales: ["en", "es", "fr"], // use your real locales
  defaultLocale: "en",
});

export const config = {
  matcher: [
    // Run middleware on all pages EXCEPT Next.js internals + public files
    "/((?!api|_next|favicon.ico|robots.txt|sitemap.xml|.*\\..*).*)",
  ],
};
